/**
 * OpenAPI 3.1 spec generated from registered routes.
 */

import "@/lib/routes/barrel";
import { router } from "@/lib/router";

export const GET = router.openapi({
  title: "AgentUpload API",
  version: "1.0.0",
  description:
    "S3-backed file uploads via x402 micropayments. USDC on Base.",
});
